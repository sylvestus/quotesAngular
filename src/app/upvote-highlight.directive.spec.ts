import { UpvoteHighlightDirective } from './upvote-highlight.directive';

describe('UpvoteHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new UpvoteHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
